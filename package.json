{
  "name": "smartcontract1202",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "test": "hardhat test",
    "build": "cd dashboard && npm install --legacy-peer-deps && npm run build && cd .. && rm -rf dist && mv dashboard/dist dist"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.22.3"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "ethers": "^6.13.2"
  }
}
